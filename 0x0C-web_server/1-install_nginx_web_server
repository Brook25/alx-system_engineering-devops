#!/usr/bin/env bash
# Install Nginx and prints a message
sudo apt update -y
sudo apt upgrade -y
sudo apt install nginx -y
sudo service nginx restart
echo "Hello World!" | sudo tee /var/www/html/index.nginx-debian.html
